# Database - SQLite (desarrollo)
# Para producción con MySQL, cambiar a: mysql://usuario:contraseña@localhost:3306/reservas_cumbres
DATABASE_URL="file:./dev.db"

# JWT - Genera uno seguro con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="cambiar-por-secret-seguro-generado"
JWT_EXPIRES_IN="7d"

# Server
PORT=3001
NODE_ENV=development

# Email (simulado)
EMAIL_FROM="noreply@reservascumbres.com"

# Uploads
UPLOAD_DIR="./uploads"
MAX_FILE_SIZE=5242880
